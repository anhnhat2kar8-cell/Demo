local Players = game:GetService("Players")
local player = Players.LocalPlayer

local AutoFarm = true

-- lấy HRP an toàn
local function getHRP()
	local char = player.Character or player.CharacterAdded:Wait()
	return char:WaitForChild("HumanoidRootPart")
end

-- tìm NPC gần nhất còn sống
local function getNPC()
	local closest = nil
	local dist = math.huge

	for _,v in ipairs(workspace:GetChildren()) do -- tối ưu hơn GetDescendants
		if v.Name == "CityNPC" then
			local hum = v:FindFirstChild("Humanoid")
			local hrp = v:FindFirstChild("HumanoidRootPart")

			if hum and hrp and hum.Health > 0 then
				local myHRP = getHRP()
				local d = (myHRP.Position - hrp.Position).Magnitude

				if d < dist then
					dist = d
					closest = v
				end
			end
		end
	end

	return closest
end

-- AUTO FARM
task.spawn(function()
	while true do
		task.wait(0.01)

		if not AutoFarm then continue end

		local char = player.Character
		if not char then continue end

		local hrp = char:FindFirstChild("HumanoidRootPart")
		if not hrp then continue end

		local npc = getNPC()

		if npc then
			local npcHRP = npc:FindFirstChild("HumanoidRootPart")
			local hum = npc:FindFirstChild("Humanoid")

			if npcHRP and hum and hum.Health > 0 then

				-- TELE LIÊN TỤC
				hrp.CFrame = npcHRP.CFrame * CFrame.new(0,3,3)

				-- đánh
				local tool = char:FindFirstChildOfClass("Tool")
				if tool then
					tool:Activate()
				end

			end
		else
			-- KHÔNG CÓ NPC → đợi respawn
			task.wait(0.5)
		end
	end
end)
