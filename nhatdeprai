local Players = game:GetService("Players")
local player = Players.LocalPlayer
local VirtualUser = game:GetService("VirtualUser")

local AutoRescue = false
local OldPos = nil
local UNDERGROUND = -10
local blockedTargets = {}

-- Anti AFK
player.Idled:Connect(function()
	VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
	task.wait(1)
	VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
end)

-- GUI
local gui = Instance.new("ScreenGui", game.CoreGui)
gui.ResetOnSpawn = false

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0,60,0,60)
frame.Position = UDim2.new(0.78,0,0.4,0)
frame.BackgroundColor3 = Color3.fromRGB(15,15,15)
frame.Active = true
frame.Draggable = true

local frameCorner = Instance.new("UICorner", frame)
frameCorner.CornerRadius = UDim.new(0,8)

local stroke = Instance.new("UIStroke", frame)
stroke.Thickness = 2

-- Viền đổi màu nhẹ
task.spawn(function()
	local hue = 0
	while true do
		hue += 0.002
		if hue > 1 then hue = 0 end
		stroke.Color = Color3.fromHSV(hue,0.7,1)
		task.wait(0.02)
	end
end)

-- chữ Tele
local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1,0,0,20)
title.BackgroundTransparency = 1
title.Text = "Tele"
title.TextColor3 = Color3.fromRGB(255,170,0)
title.TextScaled = true
title.Font = Enum.Font.GothamBold

-- switch nền
local switchBg = Instance.new("Frame", frame)
switchBg.Size = UDim2.new(0,40,0,18)
switchBg.Position = UDim2.new(0.17,0,0.58,0)
switchBg.BackgroundColor3 = Color3.fromRGB(0,220,220)

local switchCorner = Instance.new("UICorner", switchBg)
switchCorner.CornerRadius = UDim.new(1,0)

-- nút tròn
local knob = Instance.new("TextButton", switchBg)
knob.Size = UDim2.new(0,18,0,18)
knob.Position = UDim2.new(0,0,0,0)
knob.Text = ""
knob.BackgroundColor3 = Color3.new(1,1,1)

local knobCorner = Instance.new("UICorner", knob)
knobCorner.CornerRadius = UDim.new(1,0)

-- bật tắt
knob.MouseButton1Click:Connect(function()
	AutoRescue = not AutoRescue

	if AutoRescue then
		knob:TweenPosition(UDim2.new(0.55,0,0,0),"Out","Quad",0.2,true)
	else
		knob:TweenPosition(UDim2.new(0,0,0,0),"Out","Quad",0.2,true)
	end
end)

-- NoClip
task.spawn(function()
	while true do
		task.wait()
		if AutoRescue and player.Character then
			for _,v in pairs(player.Character:GetDescendants()) do
				if v:IsA("BasePart") then
					v.CanCollide = false
				end
			end
		end
	end
end)

-- tìm prompt cứu
local function findPrompt()
	for _,v in pairs(workspace:GetDescendants()) do
		if v:IsA("ProximityPrompt")
		and v.ActionText == "Cứu"
		and not blockedTargets[v] then

			local char = v:FindFirstAncestorOfClass("Model")

			if char and char:FindFirstChild("HumanoidRootPart") then
				return v, char.HumanoidRootPart
			end
		end
	end
end

task.spawn(function()
	while task.wait(0.25) do
		if AutoRescue and player.Character then

			local hrp = player.Character:FindFirstChild("HumanoidRootPart")
			local prompt, targetHRP = findPrompt()

			if hrp and prompt and targetHRP then

				OldPos = hrp.CFrame
				prompt.MaxActivationDistance = 50

				hrp.CFrame = CFrame.new(
					targetHRP.Position.X,
					targetHRP.Position.Y + UNDERGROUND,
					targetHRP.Position.Z
				)

				task.wait(0.2)

				local success = pcall(function()
					fireproximityprompt(prompt)
				end)

				if not success then
					blockedTargets[prompt] = true
					task.delay(10,function()
						blockedTargets[prompt] = nil
					end)
				end

				task.wait(0.35)

				if OldPos then
					hrp.CFrame = OldPos
				end
			end
		end
	end
end)
