local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/shlexware/Orion/main/source"))()
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Backpack = LocalPlayer:WaitForChild("Backpack")

-- GUI
local Window = OrionLib:MakeWindow({Name = "framboss | renx", HidePremium = false, SaveConfig = true, IntroEnabled = false})
local Tab = Window:MakeTab({Name = "framboss", Icon = "rbxassetid://4483345998", PremiumOnly = false})

-- Biến chung
local selectedWeapon = nil
local autoFarm = false
local autoHeal = false
local autoPickup = false
local hpThreshold = 60

-- Dropdown chọn vũ khí
Tab:AddDropdown({
    Name = "Chọn Vũ Khí",
    Default = nil,
    Options = {},
    Callback = function(Value)
        selectedWeapon = Value
        OrionLib:MakeNotification({
            Name = "Vũ khí đã chọn",
            Content = Value,
            Time = 3
        })
    end
})

-- Cập nhật danh sách vũ khí liên tục
task.spawn(function()
    while task.wait(2) do
        local weapons = {}
        for _, tool in ipairs(Backpack:GetChildren()) do
            if tool:IsA("Tool") then
                table.insert(weapons, tool.Name)
            end
        end
        Tab:GetChildren()[1]:Refresh(weapons) -- cập nhật dropdown
    end
end)

-- Nút bật fram boss
Tab:AddButton({
    Name = "Bật script fram boss",
    Callback = function()
        autoFarm = not autoFarm
        OrionLib:MakeNotification({
            Name = "Trạng Thái Fram Boss",
            Content = autoFarm and "Đã bật" or "Đã tắt",
            Time = 3
        })
    end
})

-- Bật auto Băng Gạc
Tab:AddToggle({
    Name = "Auto Băng Gạc dưới 60 hp",
    Default = false,
    Callback = function(Value)
        autoHeal = Value
    end
})

-- Auto lụm đồ
Tab:AddToggle({
    Name = "Auto Lụm Đồ",
    Default = false,
    Callback = function(Value)
        autoPickup = Value
    end
})

-- Slider điều chỉnh % máu để hồi
Tab:AddSlider({
    Name = "🔁 Tầm nhặt",
    Min = 10,
    Max = 200,
    Default = 50,
    Color = Color3.fromRGB(0,255,140),
    Increment = 1,
    ValueName = "Stud",
    Callback = function(Value)
        hpThreshold = Value
    end
})

-- Hiển thị % máu Boss
local BossHPBar = Tab:AddParagraph({
    Title = "Thanh Máu Boss",
    Content = "Đang kiểm tra..."
})

-- Hàm tìm Boss
local function getBoss()
    for _, obj in ipairs(workspace:GetChildren()) do
        if obj:IsA("Model") and obj:FindFirstChild("Humanoid") and obj.Name:lower():find("boss") then
            return obj
        end
    end
end

-- Auto hành động
RunService.RenderStepped:Connect(function()
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end

    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid then return end

    -- Auto heal
    if autoHeal and humanoid.Health < hpThreshold then
        for _, item in ipairs(Backpack:GetChildren()) do
            if item.Name:lower():find("băng") then
                item:Activate()
                task.wait(0.5)
            end
        end
    end

    -- Auto farm boss
    if autoFarm then
        local boss = getBoss()
        if boss and boss:FindFirstChild("HumanoidRootPart") then
            local dist = (boss.HumanoidRootPart.Position - character.HumanoidRootPart.Position).Magnitude
            if dist > 5 then
                character:PivotTo(boss.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3))
            end

            if selectedWeapon and Backpack:FindFirstChild(selectedWeapon) then
                Backpack[selectedWeapon].Parent = character
            end
        end
    end

    -- Auto lụm
    if autoPickup then
        for _, item in ipairs(workspace:GetDescendants()) do
            if item:IsA("Tool") and (item.Position - character.HumanoidRootPart.Position).Magnitude < 15 then
                firetouchinterest(item.Handle, character.HumanoidRootPart, 0)
                firetouchinterest(item.Handle, character.HumanoidRootPart, 1)
            end
        end
    end

    -- Update % máu Boss
    local boss = getBoss()
    if boss and boss:FindFirstChild("Humanoid") then
        local current = boss.Humanoid.Health
        local max = boss.Humanoid.MaxHealth
        local percent = math.floor((current / max) * 100)
        local filled = math.floor(percent / 5)
        local empty = 20 - filled
        local bar = string.rep("▓", filled) .. string.rep("░", empty)
        BossHPBar:Set({
            Title = "Máu Boss",
            Content = string.format("%s %d%%", bar, percent)
        })
    else
        BossHPBar:Set({
            Title = "Máu Boss",
            Content = "Không tìm thấy Boss"
        })
    end
end)

-- Khởi động GUI
OrionLib:Init()
