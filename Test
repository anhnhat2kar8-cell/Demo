-- Ultra Obfuscated Roblox Script
local function xor_decode(s,key)
    local t={}
    for i=1,#s do
        t[i]=string.char(s:byte(i)~key)
    end
    return table.concat(t)
end

-- Toàn bộ script đã nén + XOR 123
local obf_code = xor_decode("\27\12\45\61\34\..." , 123)

-- Chạy script
local run = loadstring or load
run(obf_code)()
